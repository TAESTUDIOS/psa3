# Copy this file to .env.local for local dev, or set these in Vercel Project Settings -> Environment Variables
# Database (Neon Postgres)
DATABASE_URL=
# or, alternatively
NEON_DATABASE_URL=

# Optional: client-side fallback chat webhook (used by the frontend when not triggering a ritual)
NEXT_PUBLIC_FALLBACK_WEBHOOK=

# Reserved for future/n8n usage (keep empty unless you wire them up)
PUSHCUT_TOKEN=
GPT_API_KEY=
